{
  "name": "hexo-site",
  "version": "0.0.0",
  "scripts": {
    "gitbook": "gitbook",
    "install": "gitbook install",
    "start": "gitbook serve",
    "build": "gitbook build"
  },
  "dependencies": {
    "gitbook-cli": "^2.3.2",
    "gitbook": "^3.2.3"
  }
}
